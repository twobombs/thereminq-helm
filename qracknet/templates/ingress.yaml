apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: qracknet
spec:
  rules:
  - host: ingress-qracknet.ddns.net
    http:
      paths:
      - path: /
        backend:
          serviceName: http
          servicePort: 6080
