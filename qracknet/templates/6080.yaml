apiVersion: extensions/v1beta1
kind: Ingress
spec:
  rules:
  - host: ingress-qracknet.ddns.net
    http:
      paths:
      - backend:
          serviceName: {{ $fullName }}
          servicePort: 6080
        path: /
